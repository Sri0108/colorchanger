name: Build, Tag, Push, and Deploy Frontend Service

permissions:
  id-token: write   # Required to request the JWT token for OIDC
  contents: read    # Required to read repository contents (checkout)
  packages: write 
on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    name: Build, Tag, Push, and Deploy Frontend Service
    runs-on: ubuntu-latest

    steps:
      # Step 1: Checkout the code from the repository
      - name: Checkout code
        uses: actions/checkout@v2

      # Step 2: Configure AWS credentials using OIDC
      - name: Configure AWS credentials with OIDC
        uses: aws-actions/configure-aws-credentials@v2
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_ARN }}  # The ARN of the IAM role with ECS and ECR permissions
          aws-region: ${{ secrets.AWS_REGION }}

      # Step 3: Verify AWS credentials
      - name: Verify AWS credentials
        run: |
          aws sts get-caller-identity

      # Step 4: Login to Amazon ECR
      - name: Login to Amazon ECR
        id: login-ecr
        run: |
          aws ecr get-login-password --region ${{ secrets.AWS_REGION }} | docker login --username AWS --password-stdin ${{ secrets.ECR_REGISTRY }}

      # Step 5: Determine the new Docker image tag
      # Determine the new Docker image tag
      - name: Determine new image tag
        id: versioning
        run: |
          # Fetch the latest tag from ECR
          LATEST_TAG=$(aws ecr describe-images \
            --repository-name ${{ secrets.ECR_REPOSITORY }} \
            --region ${{ secrets.AWS_REGION }} \
            --query "images | sort_by(@, &imagePushedAt) | [-1].imageTags[0]" \
            --output text || echo "None")

          # Default tag if no images exist
          if [ "$LATEST_TAG" == "None" ] || [ -z "$LATEST_TAG" ]; then
            LATEST_TAG="dev-ui.v1.0.0"
          fi

          # Parse and increment tag
          if [[ "$LATEST_TAG" =~ dev-ui.v([0-9]+).([0-9]+).([0-9]+) ]]; then
            MAJOR=${BASH_REMATCH[1]}
            MINOR=${BASH_REMATCH[2]}
            PATCH=${BASH_REMATCH[3]}
            NEW_TAG="dev-ui.v${MAJOR}.${MINOR}.$((PATCH + 1))"
          else
            NEW_TAG="dev-ui.v1.0.0"
          fi

          # Trim any spaces or newlines
          NEW_TAG=$(echo "$NEW_TAG" | tr -d '[:space:]')

          # Validate the tag
          if [[ -z "$NEW_TAG" || "$NEW_TAG" == *" "* || "$NEW_TAG" == *":"* ]]; then
            echo "Invalid NEW_TAG. Using fallback: dev-ui.v1.0.0"
            NEW_TAG="dev-ui.v1.0.0"
          fi

          echo "NEW_TAG=$NEW_TAG" >> $GITHUB_ENV
          echo "NEW_TAG determined: $NEW_TAG"

# Verify NEW_TAG before build
      - name: Verify NEW_TAG
        run: echo "Using image tag:$NEW_TAG"

      # Build Docker image
      - name: Build Docker image
        run: |
          IMAGE_TAG=${NEW_TAG}
          if [[ -z "$IMAGE_TAG" ]]; then
            echo "Error: IMAGE_TAG is empty!" && exit 1
          fi
          docker build -t devi:$IMAGE_TAG .  
          echo "Built Docker image: devi:$IMAGE_TAG"

      # Push Docker image to ECR
      - name: Push Docker image to ECR
        run: |
          IMAGE_TAG=${NEW_TAG}
          IMAGE_URI=${{ secrets.ECR_REGISTRY }}/${{ secrets.ECR_REPOSITORY }}:$IMAGE_TAG
          docker tag devi:$IMAGE_TAG $IMAGE_URI  
          docker push $IMAGE_URI
          echo "Pushed Docker image: $IMAGE_URI"
          echo "IMAGE_URI=$IMAGE_URI" >> $GITHUB_ENV

      # Step 10: Register a new ECS Task Definition Revision
      - name: Register ECS Task Definition
        id: register-task-def
        run: |
          EXISTING_TASK_DEF=$(aws ecs describe-task-definition \
            --task-definition ui \  # Hardcoded task definition name 'ui'
            --query taskDefinition \
            --output json)
          UPDATED_TASK_DEF=$(echo $EXISTING_TASK_DEF | jq --arg IMAGE_URI "$IMAGE_URI" '.containerDefinitions[0].image = $IMAGE_URI')
          echo $UPDATED_TASK_DEF | jq 'del(.status, .revision, .taskDefinitionArn, .registeredAt, .registeredBy)' > new-task-def.json
          aws ecs register-task-definition --cli-input-json file://new-task-def.json

      # Step 11: Deploy the ECS service with the new task definition
      - name: Deploy to ECS Frontend Service
        run: |
          NEW_TASK_DEF_ARN=$(aws ecs describe-task-definition \
            --task-definition ui \  # Hardcoded task definition name 'ui'
            --query 'taskDefinition.taskDefinitionArn' --output text)
          aws ecs update-service \
            --cluster ${{ secrets.ECS_CLUSTER }} \
            --service ${{ secrets.ECS_SERVICE }} \
            --task-definition $NEW_TASK_DEF_ARN \
            --force-new-deployment
